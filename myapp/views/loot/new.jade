extends ../layout

block content
  .container
    .row
      .text.center
        h2 New loot
      form.for-horizontal(action='/loot/new', method = 'POST')
        .form-group
          label.col-sm-2.label-control(for='player') Player
        select
          option(value='sunwell') Sunwell